export interface IcolorSection {
    start: number,
    end: number,
    firstColor: colorObject,
    secondColor: colorObject,
    thirdColor: colorObject
}

export interface colorObject {
    r: number,
    g: number,
    b: number
}
